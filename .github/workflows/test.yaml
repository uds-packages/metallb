# Copyright 2024 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

name: Test
on:
  # This workflow is triggered on pull requests to the main branch.
  pull_request:
    # milestoned is added here so that a PR can be re-triggered if it is milestoned.
    types: [milestoned, opened, reopened, synchronize]
# Permissions for the GITHUB_TOKEN used by the workflow.
permissions:
  contents: read # Allows reading the content of the repository.
  packages: read # Allows reading the content of the repository's packages.
  pull-requests: read
  id-token: write
# Abort prior jobs in the same workflow / PR
concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check-flavor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4.3.0
      - name: test-flavor
        uses: defenseunicorns/uds-common/.github/actions/test-flavor@f38e2d9595351ba8f9455f201a66eb91ccd039f2 # v1.20.3
        id: test-flavor
    outputs:
      upgrade-flavors: ${{ steps.test-flavor.outputs.upgrade-flavors }}
  validate:
    needs: check-flavor
    uses: defenseunicorns/uds-common/.github/workflows/callable-test.yaml@f38e2d9595351ba8f9455f201a66eb91ccd039f2 # v1.20.3
    secrets: inherit # Inherits all secrets from the parent workflow.
    strategy:
      fail-fast: true
      matrix:
        # type: [install, upgrade]
        type: [install]
        flavor: [upstream, unicorn, registry1]
    with:
      upgrade-flavors: ${{ needs.check-flavor.outputs.upgrade-flavors }}
      flavor: ${{ matrix.flavor }}
      type: ${{ matrix.type }}
      options: --set BASE_REPO="ghcr.io/uds-packages"
      runsOn: appstore-4-core-amd64
  verify-test:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name: Success
        run: echo "All tests passed successfully!"
